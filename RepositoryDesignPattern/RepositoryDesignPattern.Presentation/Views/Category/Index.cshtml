@model List<RepositoryDesignPattern.Entity.Concrete.Category>

@{
    Layout = null;
}

<link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />

<div class="container mt-5 w-50">
    <div class="jumbotron">
        <a href="/Category/Add" class="btn btn-info mb-3">Ekle</a>
        <table class="table table-hover text-center align-middle">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Kategori Adı</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Name</td>
                        <td>
                            <a href="/Category/Delete/@item.Id" class="btn btn-danger">Sil</a>
                            <a href="/Category/Edit/@item.Id" class="btn btn-success">Güncelle</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>